{
  "name": "js-tryouts-engine",
  "version": "1.0.0",
  "description": "Js Tryouts",
  "main": "index.js",
  "type": "module",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "nodemon --env-file=.env index.js",
    "local": "nodemon --env-file=.env.local index.js",
    "telegram:start": "nodemon --env-file=.env telegram.js",
    "telegram:local": "nodemon --env-file=.env.local telegram.js"
  },
  "author": "foreverabhi",
  "license": "ISC",
  "dependencies": {
    "node-telegram-bot-api": "^0.66.0"
  }
}